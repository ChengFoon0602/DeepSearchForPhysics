# Deep Search Pro · 从零到一学习日记

> 2026-08-06 ~ 08-07 · 一次完整的多智能体项目搭建 + 深度 Debug
> 目的：记录"做了什么"之外，更记录"**怎么发现、怎么定位、怎么修**"——这部分才是可迁移的经验。

---

## 目录
1. [总览：今天做了什么](#一总览今天做了什么)
2. [从 0 到 1 搭建](#二从-0-到-1-搭建)
3. [踩坑实录：21 个问题的排查与 Debug](#三踩坑实录21-个问题的排查与-debug)
4. [Debug 方法论总结（可迁移）](#四debug-方法论总结可迁移)
5. [知识点沉淀](#五知识点沉淀)
6. [遗留与下一步](#六遗留与下一步)
7. [本地 RAG 知识库：把 RAGFlow 换成真能跑的管线](#七本地-rag-知识库把-ragflow-换成真能跑的管线)
8. [进阶一：Agent 评测体系 + 深度搜索循环](#八进阶一agent-评测体系--深度搜索循环)
9. [进阶二：人工审批节点 HITL](#八6-人工审批节点-hitl生成报告前人类确认)
10. [PDF 公式渲染失败排查（止损教训）](#八7-pdf公式渲染失败排查word-com-不认-img及时止损的教训)

---

## 一、总览：今天做了什么

| 阶段 | 内容 | 状态 |
|------|------|------|
| 搭建 | venv + 依赖 + .env + 启动 + 首次测试 | ✅ |
| 自定义 | 新增「物理文献与公式验证助手」子智能体 | ✅ |
| 提示词工程 | 修公式渲染 / 修默认文件生成 / 修 prompt 自相矛盾 | ✅ |
| 前端 | 聊天页 + WebSocket 实时进度 + 文件下载 + 精美化 | ✅ |
| 持久化 | InMemorySaver → AsyncSqliteSaver | ✅ |
| 行为修复 | 网络搜索不再被跳过（强制多源检索）；公式分隔符统一 `$...$`；清掉 prompts.py 调试 print | ✅ |
| 进阶 | Agent 评测体系（`eval/`，3 case）+ 网络搜索升级深度检索循环（精读/补检索/带来源汇总）+ 公式分隔符确定性归一化 | ✅ |
| HITL | 人工审批节点：生成报告前中断等审批（approve/reject/edit 三态），前端「报告审批」开关 | ✅ |

---

## 二、从 0 到 1 搭建

### 2.1 搭建速查（可直接复用）

```powershell
cd D:\桌面文件\大三\agent\deep-search-pro-master
python -m venv .venv
.venv\Scripts\Activate.ps1
# 若被执行策略拦截：
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.venv\Scripts\Activate.ps1

python -m pip install -r requirements.txt   # 135 个包，3~10 分钟

Copy-Item .env.example .env
# 只填 4 行：
#   OPENAI_BASE_URL=https://api.deepseek.com
#   OPENAI_API_KEY=sk-xxx
#   LLM_QWEN_MAX=deepseek-chat   ← 变量名是代码写死的，值填模型名
#   TAVILY_API_KEY=tvly-xxx

python -c "from agent.main_agent import main_agent; print('OK')"  # 30 秒自检
python -m api.server    # ★ 不是 README 的 python api/server.py！
```

### 2.2 关键文件地图

| 文件 | 作用 |
|------|------|
| `agent/llm.py` | 模型初始化，10 行，读环境变量 |
| `prompt/prompts.yml` | **所有提示词都在这**，改行为先改这里 |
| `agent/main_agent.py` | 主智能体 + 懒加载工厂 `get_main_agent()` |
| `agent/subagents/*.py` | 子智能体 = 一个字典（name/description/system_prompt/tools） |
| `api/server.py` | FastAPI 路由 + WebSocket + 静态挂载 |
| `api/monitor.py` | 工具调用埋点 → WebSocket 推送 |
| `api/context.py` | ContextVar 协程级数据隔离（多用户不串台） |
| `tools/*.py` | 6 个 `@tool` |
| `web/index.html` | 前端聊天页 |

### 2.3 两个"README 没告诉你"的硬事实

1. **启动命令是错的**：README 写 `python api/server.py`。实际跑会 `ModuleNotFoundError: No module named 'agent'`。
   - 原因：`python 脚本路径` 时 `sys.path[0]` 指向脚本所在目录（`api/`），项目根目录不在 path 上。
   - 正确：`python -m api.server`（`-m` 以当前目录为基准）。
   - 排查手段：5 秒实证——建一个 `api/server.py` 里 `import agent` 的最小复现，两种启动方式各跑一次，肉眼确认。

2. **"数据库/RAGFlow 可选"只指配置可选，不是依赖包可选**：`main_agent.py` import 时就硬加载 `mysql.connector`、`ragflow_sdk`、`tavily`、`langchain-openai`。所以包必须全装，`.env` 的 key 才可以不填。

---

## 三、踩坑实录：21 个问题的排查与 Debug

> 每个问题都按「现象 → 排查 → 根因 → 解决 → 方法论」五段写。方法论那行是能迁移的。

### 问题 1：主智能体"就绪待命"，不干活

- **现象**：发"搜索一下最近 AI Agent 领域的最新进展"，模型回"工作目录已就绪，当前为空目录，等待任务"，没调用任何工具，也没生成文件。
- **排查**：看服务器日志——`session_created` 有、`loop: True` 有，但**没有任何 `assistant_call` / `tool_start`**。
- **根因**：`run_deep_agent` 把一段【工作环境指令】追加到**每次** query 后面，内容写得像系统提示词（"系统会提供工作目录…规则 1-4"）。模型把整条消息误读成"初始化握手"，于是自我介绍、等待任务。
- **解决**：换更明确、带产出物的 query（也建议走 Swagger，避免 PowerShell 中文编码干扰）。
- **方法论**：**用"有没有调用工具"来区分「链路问题」还是「模型行为问题」**。链路问题看事件流是否完整；模型行为问题看它到底听懂了什么。别急着改代码，先判断是哪一类。

### 问题 2：生成的 Markdown 里公式渲染不出来

- **现象**：md 文件里 `\(T = 2\pi\sqrt{I/(mgd)}\)` 以原文显示。
- **排查**：**打开生成的文件看真实内容**（而不是猜）——LaTeX 语法完全正确，问题出在**分隔符**。
- **根因**：模型输出的是 LaTeX 原生分隔符 `\(...\)` / `\[...\]`，但 Markdown 生态的渲染器（Typora、VS Code 预览）只认 MathJax 风格的 `$...$` / `$$...$$`。
- **解决**：prompts.yml 加规则"行内公式用 `$...$`，独立公式用 `$$...$$`，严禁 `\(...\)`"。网页端则引入 KaTeX + marked 渲染。
- **方法论**：**区分"语法"和"分隔符"两个层面**。语法错是模型没写对；分隔符错是格式约定不匹配。看产物、看渲染器的约定，别先怀疑 LaTeX 本身。

### 问题 3：没要求，却自动生成并转 PDF

- **现象**：query 只让验证公式，agent 却生成 MD + 转 PDF。
- **排查**：读 main_agent 的 system_prompt——"文件生成"规则写得非常重（"你可以生成 Markdown、PDF"），而"没明确要求就文本回复"只出现一次、无约束力。
- **根因**：**prompt 的权重分配决定了模型的默认行为**。规则多、语气强的一方赢。
- **解决**：在【关键执行顺序】加硬规则（"严禁自动转 PDF"），并让"默认生成 MD"成为条件触发（涉及公式/综述才生成，简单问答直接回复）。
- **方法论**：模型默认行为 = 提示词里哪条规则**说得最重**。要改变默认行为，用【关键执行顺序】/"严禁"这类强约束，别指望一条轻描淡写的规则。

### 问题 4：加了规则，模型还是随机站队（本次最核心）

- **现象**：改完默认生成 MD 的规则后，重发复摆 query，**又不生成文件了**。
- **排查**：读 prompts.yml **全文**——不是只看自己改的部分。发现第 27 行：*"你**没有生成文件的能力**，只能交给文件生成助手去解决"*。但：团队里根本没有"文件生成助手"；第 30 行又说"你掌握的工具包括 generate_markdown"。
- **根因**：**同一个 prompt 内自相矛盾**（一句说"你不能生成"，一句说"你有生成工具"）。模型面对矛盾规则是**随机站队**的——所以它第一次生成、第二次不生成，行为不稳。
- **解决**：删掉"你没有生成文件的能力"，统一为"所有文档由你自己生成"。顺带修了另外两处引用不存在的"文件生成助手"的地方，团队数量改成"多个"。
- **方法论**：**prompt 自相矛盾 → 模型随机站队，这是行为不稳的第一嫌疑**。修行为问题，先通读整个 prompt 找自洽性，别在矛盾之上叠新规则。

### 问题 5：DeepSeek 网络瞬断（上游故障）

- **现象**：服务器控制台 `[Monitor:error] 执行主智能发生异常信息：('Connection aborted.', RemoteDisconnected('Remote end closed connection without response'))`。
- **排查**：`RemoteDisconnected` 是"连接被对端关闭、无响应"，是**网络层**错误，不是 401/404（那是鉴权/模型问题）。重试一次即好 → 确认是瞬断。
- **根因**：DeepSeek 服务端抖动 / 网络瞬断 / 长连接复用被服务端关闭。
- **解决**：重试。若频繁，给 `llm.py` 的 `init_chat_model` 加 `max_retries=3, timeout=60`（只重试 HTTP 请求本身，幂等安全）。
- **方法论**：**先区分上游错误和代码 bug**。网络层错误（RemoteDisconnected/Timeout）先重试；如果重试就好，别改代码。要区分：401/403=鉴权，404=配置错，RemoteDisconnected=网络。

### 问题 6：下载列表混入上次的报告

- **现象**：前端下载栏同时显示这次和上次生成的报告。
- **排查**：看 `main_agent.py` —— `session_dir = output/session_{thread_id}`，**按 thread_id 建目录**；前端把同一个 thread_id 存在 localStorage 反复复用 → 同一目录累积所有历史文件。
- **根因**：**会话目录是"工作区"语义（一个线程一个目录），不是"单次任务"语义**。`/api/files` 列目录里所有文件。
- **解决**：前端记录 `taskStart`，`loadFiles` 只显示 `mtime >= taskStart` 的文件。展示层过滤，服务端不动。
- **方法论**：**先理解设计语义再决定在哪层修**。这里是"工作区"语义，行为合理，只是展示层需求——所以改前端过滤，而不是改后端目录结构。

### 问题 7：静态挂载后 API 的 405 变成了 404

- **现象**：加了 `app.mount("/", StaticFiles(...))` 后，`GET /api/task` 从预期的 405 变成 404。
- **排查**：用"错误方法"访问 API 路径做测试，暴露了路由优先级问题。
- **根因**：Starlette 匹配路由时，路径对但方法不对 → `Match.PARTIAL`，路由器**继续往后找 full match** → 被 `"/"` 兜底挂载接住 → 当静态文件返回 404。
- **解决**：不需要修——真实调用（方法正确）路径先 full match，轮不到挂载。只是"用错方法"的边角从 405 变 404。
- **方法论**：**用"错误方法"测试会暴露路由匹配细节**。挂载 `/` 兜底是常见做法，但要理解它和 partial match 的交互。

### 问题 8：SqliteSaver 三连坑（本次最硬）

- **现象与排查**，一步一步来：

| 尝试 | 报错 | 说明 |
|------|------|------|
| ① 同步 `SqliteSaver` + `check_same_thread=False` | `The SqliteSaver does not support async methods. Consider using AsyncSqliteSaver` | 图用 `astream()` 异步执行，同步 saver 不支持异步方法 |
| ② `AsyncSqliteSaver` 在模块级建 | `RuntimeError: no running event loop` | `AsyncSqliteSaver.__init__` 里调用 `asyncio.get_running_loop()`——**必须在运行中的事件循环里构造** |
| ③ `asyncio.run(aiosqlite.connect(...))` | `ValueError: a coroutine was expected` | `aiosqlite.connect()` 返回可 await 的 Connection，不是 coroutine，`asyncio.run` 不吃；包一层 `await` 才连上 |
| ④ 建好后 CLI 跑完不退 | 进程挂住 | aiosqlite 连接的后台线程是**非守护线程**，主线程结束要等它（服务器不受影响） |

- **根因**：AsyncSqliteSaver 的设计约束是"必须活在事件循环里"。这和项目"模块级构建图"的结构冲突。
- **解决**：把图的构建从模块级改成**懒加载**——`get_main_agent()` 在第一次跑任务时（已在 uvicorn 事件循环内）建连接、建图，加锁防并发双建。
- **验证**：真实 uvicorn 服务器上跑通——报告生成 + `checkpoints` 表 40 行落盘 + 重启后记忆还在。
- **方法论**：**框架报错要认真读，报错本身就是答案**。这个报错直接告诉你该用哪个类、约束是什么。看 API 用 `inspect.signature()` 确认，别猜。**架构约束（要 running loop）不是 bug，是设计——它逼你把"模块级构建"改成"循环内懒加载"，这是正确方向。**

### 问题 9：前端"记录"按钮点击没反应（异步状态卡死）

- **现象**：发送按钮点击无任何反应，Enter 也不触发。
- **排查**：先读代码确认接线在（`sendBtn.onclick = send` 存在、脚本没有中途抛出）→ 再追状态流转——发现 `busy` 只有 `task_result` / `error` 两个事件能重置，但页面**没有 `onclose` 处理、没有超时兜底**。
- **根因**：`send()` 里 `busy = true; 按钮 disabled` 之后，若 WebSocket 中途断开（调试时重启过服务器 / 网络抖动），两个终态事件都不会来 → `busy` 永远为 `true` → 按钮永远 disabled。**连接断开是静默的，页面根本不知道**。
- **解决**：四个兜底——① `ws.onclose` 里 `if(busy) finish()` 并提示"连接已断开"；② `ws.onerror` 同样重置；③ 5 分钟看门狗 `setTimeout`（`finish()` 时清除）；④「新会话」也重置 busy。
- **方法论**：**异步状态必须有"终态恢复路径"**（onclose / onerror / 超时看门狗），否则状态会静默卡死。设计任何前端异步流程，问自己三个问题：正常完成怎么办？报错怎么办？**断连 / 无响应怎么办**？——最后那个是永远要补的兜底。

### 问题 10：前端"发送"按钮没反应——两个独立问题叠加

> 注：问题 9 的 busy 恢复路径是值得保留的健壮性改进（防御性），但并非本次"没反应"的直接原因。Playwright 排查 + 用户澄清后，真相是两件事叠加。

- **现象**：点「记录」按钮没反应。
- **排查**：
  - **用户澄清（实际遇到）**：按钮文案「记录」误导，以为是"记录"按钮；且**输入框是空的**。`send()` 开头 `if (!q || busy) return`——**空输入直接静默返回**，什么都不发生。
  - **Playwright 独立发现（真实问题）**：`file://` 加载页面 → 脚本执行、点击后用户消息出现 → 前端代码正常；全新 8013 实例 → 点击 → `POST 200` → 链路正常；访问 8000 → 浏览器请求挂起 → **8000 实例坏**。
- **根因**：① 空输入 + 误导性文案（用户实际体验）；② SqliteSaver 调试期留下的坏服务器实例（独立、真实）。
- **解决**：① 按钮文案「记录」→「发送」；② 干净重启服务器（叉掉窗口 → 确认端口释放 → `python -m api.server`）。
- **方法论**：**"没反应"先查输入是否为空、按钮语义是否清晰**——`if (!q) return` 守卫对空输入是静默的，配上误导文案就是"点了没反应"。**再分清客户端 vs 服务器侧**：纯客户端按钮（#new）能用、触发请求的按钮（#send）不能用 → 问题划到服务端。**验证前端用 `file://` 绕过服务器；对比"全新实例 vs 当前实例"定位代码还是环境。**

### 问题 11：agent 记不住之前的会话——记忆是线程级的

- **现象**：新开一个会话问 agent"之前做到哪了"，它回"工作目录为空，找不到任何记录"。
- **排查**：它检查的是 `output/session_{新thread_id}`——新线程刚建的空目录；检查点表里确实没有这个线程的历史。
- **根因**：**agent 的记忆是"线程级"的**：只有当前 `thread_id` 的检查点 + 当前工作目录。新线程 = 全新上下文 + 空目录。跨线程隔离是刻意的安全设计（防止 agent 乱翻别的会话的文件）。
- **解决**：同一线程内继续（不点「新会话」，SqliteSaver 保证跨重启不丢）；跨线程续接需要额外功能（给 agent 加"查看历史输出"工具，会削弱隔离，不建议）。
- **方法论**：**理解系统的"记忆边界"**——它记住什么、不记住什么，是设计决策不是 bug。诊断"为什么记不住"，先确认边界在哪一层（线程级 / 会话级 / 全局级）。

### 问题 12：新会话找不到项目上下文——缺 CLAUDE.md

- **现象**：项目目录里新开一个 Claude 会话，它不知道项目是什么、怎么启动。
- **排查**：项目根目录没有 CLAUDE.md；`output/` 里十几个 session 目录混着空目录和带文件的，新会话无法从磁盘推断项目脉络。
- **根因**：**"约束先行"原则没落实**——搭了整个项目却没写项目说明书。
- **解决**：补一个项目 CLAUDE.md（项目是什么、启动命令、文件地图、已知坑、学习日记位置）。
- **方法论**：**可复用项目的第一个交付物不是代码，是 CLAUDE.md**。它让任何未来的会话（包括几个月后的自己）一进目录就能接上，不靠对话记忆。

### 问题 16：模型只调了知识库，直接跳过网络搜索

- **现象**：问"复摆周期公式符号含义"，agent 只调了知识库检索助手就出报告——报告自述"全部相关命中内容均来自《复摆动力学笔记.md》"，网络搜索完全没触发。
- **排查**：读生成的报告（产物）确认只用了一种信息源；再读 main_agent 的 system_prompt，发现信息获取规则原文是"**可以尝试**使用三种方式获取信息，如果边界并不明确，则全部使用"。
- **根因**：**柔性措辞 = 没有约束**。"可以尝试"是建议不是要求，模型有权挑最省事的路——库里能查到就不上网搜。
- **解决**：改成【强制要求】：凡涉及公开背景知识/外部信息/需验证佐证的知识点，必须调用**网络搜索助手**；专有资料必须调**知识库检索助手**；边界不明或都可能相关时**必须全部调用**，禁止单一信息源作答。重测后两个节点同时点亮，确认修复。
- **方法论**：**"应该/可以/尝试"这类柔性词在 prompt 里等于没说**。要约束模型行为，用"必须/严禁/不得"强制句式并写明触发条件。改行为问题时，先问自己：这条规则是"可做可不做"还是"必须做"？

### 问题 17：md 里独立公式用了 `$$...$$`，外部渲染器不识别

- **现象**：生成的报告在 Typora 里公式显示成原文。
- **排查**：打开生成文件看真实内容——独立公式写成了 `$$...$$`。前端网页（KaTeX）同时支持 `$$` 和 `$`，但用户的外部 md 渲染器（Typora 等）只认 `$...$`。
- **根因**：**一份 md 要跨多个渲染器，而各渲染器对分隔符的支持不一致**。模型按 LaTeX 惯例写 `$$`，恰好撞上最挑剔的那个渲染器。
- **解决**：prompts.yml 主智能体 + 物理文献助手统一"一律 `$...$`，严禁 `$$...$$`、`\(...\)`、`\[...\]`"。
- **方法论**：**跨工具产出的格式约定，以"最不宽容的消费方"为准**。定格式前先列：这份文件会被哪些工具打开？前端能渲染不等于文件没问题。

### 问题 18：taskkill 杀了 reloader，孤儿 worker 仍占着端口

- **现象**：`taskkill /PID` 杀旧服务器提示 SUCCESS，但 8000 端口仍 LISTENING，`netstat` 报的 OwningProcess 在 `Get-Process` 里查不到。
- **排查**：`Get-CimInstance Win32_Process` 看 python 进程命令行——uvicorn 是 **reloader(父) + worker(子) 双进程**；被杀父进程的子进程（worker）变成孤儿继续监听端口。netstat 报的 PID 正是这个孤儿。
- **根因**：`uvicorn --reload` 的双进程结构，taskkill 只杀了 reloader 父进程，worker 成孤儿存活。
- **解决**：按 ParentProcessId 找到孤儿 worker，`Stop-Process -Force`，端口释放后干净重启。
- **方法论**：**杀 uvicorn（reload 模式）要连 worker 一起杀**。"SUCCESS"提示不可信；端口占用且 PID 对不上进程列表时，用 Win32_Process 看 CommandLine 和 ParentProcessId 定位，别急着猜。

---

## 四、Debug 方法论总结（可迁移）

1. **先定位问题层级**：是「链路问题」（事件流不完整）还是「模型行为问题」（模型没按预期做事）？链路问题看日志事件，模型问题看 prompt 和产物。
2. **看真实产物，不要只看日志**：公式渲染问题，打开生成的 md 文件看内容；文件生成问题，看 output 目录里到底有没有文件。
3. **读完整 prompt，别只读自己改的部分**：模型行为不稳，第一嫌疑是 prompt 自相矛盾（模型对矛盾规则随机站队）。
4. **框架报错 = 答案**：认真读报错原文，它通常直接告诉你该用什么类、什么约束。用 `inspect.signature()` 确认 API。
5. **5 秒实证 > 猜**：sys.path 行为、路由优先级这类，写个最小复现跑一下，结论靠实验不靠记忆。
6. **区分上游错误和代码 bug**：网络层错误先重试；重试就好 = 瞬断，别改代码。
7. **测试要覆盖"错误用法"**：用错误方法访问 API 会暴露路由优先级，用 TestClient 能暴露 TestClient 独有的问题（记得和真实服务器对照）。
8. **一个原因一个原因排除**：SqliteSaver 的报错链是逐步收窄的——每修一个，报错就前进到下一个更具体的约束。
9. **异步状态必须设计终态恢复路径**：onclose / onerror / 超时看门狗。前端异步流程问自己三个问题——正常完成怎么办？报错怎么办？**断连 / 无响应怎么办**？最后一个是永远要补的兜底。
10. **前端"没反应"先分客户端 vs 服务器侧**：纯客户端按钮能用、触发请求的按钮不能用 → 问题在服务端。验证前端用 `file://` 绕过服务器，对比"全新实例 vs 当前实例"定位代码还是环境。
11. **理解系统的"记忆边界"**：记住什么、不记住什么，是设计决策。诊断"记不住"，先确认边界在哪一层（线程级 / 会话级 / 全局级）。
12. **新项目先写 CLAUDE.md**：可复用项目的第一个交付物，让任何未来会话一进目录就接上。
13. **柔性词 = 没约束**：prompt 里"应该/可以/尝试"等于没说，要强制行为用"必须/严禁/不得"+ 明确触发条件。模型行为不稳时，先检查规则是"可做可不做"还是"必须做"。
14. **跨渲染器格式以最挑剔的消费方为准**：一份 md 被前端和外部工具共同消费时，按 Typora 这类不宽容工具的约定写，别按 LaTeX 惯例。
15. **uvicorn reload 是双进程，杀父进程留孤儿 worker**：端口占用且 PID 对不上进程列表，用 `Get-CimInstance Win32_Process` 看 CommandLine/ParentProcessId 定位。
16. **评测先行**：历史 bug 先固化成自动化断言（用当前行为跑绿），再动代码——任何改动都有回归保护。本次深度搜索改造就是评测先立、改造后跑，立刻抓到一个回归（问题 19）。
17. **"必须保证"的产出物格式用代码兜底，不靠 prompt**：prompt 是概率性服从，管"尽量"；工具层确定性归一化管"一定"。两者分工，别混。
18. **含 `$`/`\`/括号的代码调试用 raw string + 独立脚本文件**：别在 shell 命令行交互式拼，`$` 会被 shell 展开、引号嵌套会踩 `unbalanced parenthesis`。
19. **长跑任务的监控要能自动恢复**：后台任务/监控可能随会话切换被终止，失败了重起重挂，别假定一次启动就有结果。

---

## 五、知识点沉淀

- **多智能体编排（orchestrator）**：主智能体不自己干活，按需求调度子智能体。deepagents 只需 name/description/system_prompt/tools 四个字段。
- **优雅降级**：README 说的"可选"——配置不填，主智能体靠 prompt 判断跳过。前提是 prompt 要诚实（不存在的服务别写进团队名单）。
- **ContextVar vs 全局变量**：FastAPI 多请求跑在同一个线程的不同协程里，全局变量会串台；ContextVar 是协程级隔离。
- **WebSocket 推送的时序**：先建连接再发任务，否则前几个事件会丢（服务端按 thread_id 查连接池）。
- **checkpointer 的三个 saver**：
  - `InMemorySaver`：内存态，重启丢；
  - `SqliteSaver`：同步，`astream` 用不了（不支持 async 方法）；
  - `AsyncSqliteSaver`：异步，**必须在事件循环里构造**。
- **checkpointer 不只存历史**：它还决定图能否从中间节点续跑——这是后面"人工审批节点"（human-in-the-loop）的基础。
- **aiosqlite 的线程模型**：连接跑在独立线程的事件循环里，不绑定调用方 loop；非守护线程会让 CLI 脚本退出时挂住。

---

## 六、遗留与下一步

- **已完成**：项目 CLAUDE.md（问题 12）；8000 服务器干净重启（问题 18）；按钮文案「记录」→「发送」+ 前端 busy 恢复路径；prompts.py 调试 print 已清；网络搜索强制调用 + 公式统一 `$...$` 已生效（2026-08-12 验证）。
- **风险提示**：问题 16 的修复是 prompt 层面的强制规则——LLM 行为是概率性的，"强制"不是 100% 保证。若想真正防回归，需要自动化评测（见下）。
- **✅ 已完成：Agent 评测体系 + 深度搜索循环**（2026-08-12，详见第八节）：`eval/` 回归评测（3 case 全 PASS）+ 网络搜索助手升级为深度检索循环（多角度检索→精读→补检索→带来源汇总）+ 生成文件公式分隔符确定性归一化。之前列的候选方向 1、2 已落地。
- **✅ 已完成：人工审批节点（HITL）**（2026-08-12，详见 8.6）：deepagents 原生 `interrupt_on` + LangGraph interrupt/resume，前端「报告审批」开关，approve/reject/edit 三态。挑战级遗留项落地，评测 auto_approve 保回归。
- **候选方向**（剩余价值排序）：
  1. **工具健壮性**：`convert_md_to_pdf` 的 Word 中转链路健壮性；`extract_web_content` 浅层模式工具层禁用的确定性省钱。
  2. **评测语料扩充**：多覆盖几个物理专题 query，把评测跑成每次改 prompt 的自动门禁（可挂 git hook / CI）。
  3. **Docker 化**：写 Dockerfile + docker-compose，一键启动所有依赖（去掉 Windows-only 的 Word COM）。
- **MySQL**：需要装服务端（MySQL 原生安装）。对简历边际价值低，暂时不装。
- **RAGFlow → 已替换**：2026-08-12 已把"注册但不可用"的 RAGFlow 子智能体替换成本地 Chroma RAG 管线，详见第七节。

---

## 七、本地 RAG 知识库：把 RAGFlow 换成真能跑的管线

> 2026-08-12 · 把项目里唯一"注册但不能用"的 RAGFlow 子智能体，换成本地 Chroma + fastembed 的完整 RAG 管线。无 Docker、无守护进程，全 pip 包。

### 7.1 为什么换

四个子智能体里只有 RAGFlow 是不可用的——要部署服务端（Docker）。而 RAG 恰恰是求职最热门的能力。方案：**本地部署 = 不部署任何服务端**，用纯 pip 包搭一条完整可演示、能讲清每个设计决策的管线：

```
文档 → 切分(chunking) → 嵌入(embedding) → 向量库存储 → 检索(top-k) → 组装上下文喂给主智能体
```

选型：**Chroma**（嵌入式向量库，API 与 Pinecone/Qdrant 一致，简历最像生产）+ **fastembed**（ONNX，不需要 PyTorch）+ **BAAI/bge-small-zh-v1.5**（中文，dim 512，~90MB，离线可用）。切分复用已有的 `langchain_text_splitters`，PDF 复用 `pypdf`，新增依赖只有 2 个包。

### 7.2 关键文件

| 文件 | 作用 |
|------|------|
| `kb/docs/` | 源文档目录（人维护）：复摆笔记 + 学习日记，做两个 demo 场景 |
| `ingest_kb.py` | 入库脚本：切分(500/80) → 嵌入 → 写 Chroma；**重跑=清空重建**（幂等） |
| `tools/rag_tools.py` | 2 个 `@tool` + 懒加载单例（Chroma collection + embedder 首次调用才建） |
| `agent/subagents/knowledge_base_agent.py` | 改挂到 `tools.rag_tools` 的两个新工具 |
| `prompt/prompts.yml` | `ragflow:` key → `kb:`，重写 description/system_prompt |

### 7.3 踩坑实录

#### 问题 13：langchain `@tool` 装饰的不是普通函数

- **现象**：自检脚本 `search_knowledge_base("复摆周期公式", top_k=3)` 直接 `TypeError: 'StructuredTool' object is not callable`。
- **排查**：报错本身就是答案——`@tool` 返回的是 `StructuredTool` 实例，不是函数。
- **解决**：用 `.invoke({'query':..., 'top_k':...})`（或 `.func(...)`）调用。
- **方法论**：**framework 包装后的对象先看类型**。别假设装饰器返回的还是原来的签名，`type()` / `dir()` 看一眼再写调用代码。

#### 问题 14：`from agent.main_agent import main_agent` 导入失败

- **现象**：按学习日记 2.1 写的自检命令跑不了——`ImportError: cannot import name 'main_agent'`。
- **根因**：模块导出的是 `_main_agent`（懒加载全局）+ `get_main_agent()` 工厂，**没有** `main_agent` 这个名字。自检命令写错了。
- **解决**：自检用 `import agent.main_agent`（能 import 就说明链路通），或 `from agent.main_agent import get_main_agent`。
- **方法论**：**文档里的命令也会过期**。重跑旧命令报错时，先读源码看导出名，别硬猜。

#### 问题 15：YAML key 改名必须和代码 import 原子落地

- **现象**：`knowledge_base_agent.py` 改成 `sub_agents_content['kb']` 后，prompts.yml 还没改前 import 直接 KeyError。
- **根因**：两个文件强耦合——代码要的 key 和 YAML 里的 key 必须同时改。
- **解决**：一次性把 `prompts.yml` 的 `ragflow:`→`kb:` 和 `knowledge_base_agent.py` 一起改完，再重启。
- **方法论**：**配置与消费它的代码是一组变更，不是两件事**。改配置必搜所有消费方（grep key 名），确认全链路一致再动。

### 7.4 本次顺带发现

- `prompt/prompts.py` 在 import 时有几行调试 `print()`，会把整个 YAML 打到控制台（服务器日志很吵）。功能无碍，可选清理。
- `学习日记.md` 2.1 的自检命令已过时（见问题 14），已修正为 `python -c "import agent.main_agent; print('OK')"`。

---

## 八、进阶一：Agent 评测体系 + 深度搜索循环

> 2026-08-12 · 两个历史 bug（跳过网络搜索、`$$...$$` 不渲染）都是**手动测试**才发现的。这次把「评测先行」落到实处：先建回归评测用现有行为跑绿，再动 prompt 升级深度搜索循环——任何改动都有回归保护。

### 8.1 为什么先评测、后改造

prompt 层的「强制规则」是概率性服从，不保证 100%。评测的价值不是替代 prompt，而是**在 prompt 失效时兜底 + 在回归发生时报警**。本次改造中评测立刻抓到一个真实回归（见问题 19），证明这套防线值得。

### 8.2 深度搜索循环（改造内容）

把「网络搜索助手」从单次搜索升级为迭代循环：

```
多角度检索(≥3) → 精读网页(extract) → 缺口分析 → 定向补检索 → 带来源汇总
```

- **`tools/tavily_tool.py`** 新增 `extract_web_content` 精读工具（`tavily_client.extract`，埋点名"网页内容精读工具"，15k 字符硬截断防上下文溢出）。
- **`prompt/prompts.yml`** `sub_agents.tavily.system_prompt` 重写为深度检索循环（广度检索→精读→缺口→补检索→汇总），预算上限 `internet_search ≤ 8`、`extract ≤ 5`。
- **挂载**：`network_search_agent.py` + `physics_lit_agent.py` 都挂精读工具（文献调研同样受益）。
- **成本控制（前端开关，8.5 详述）**：深度循环很费 Tavily credit，加前端「深度调研」开关——默认关（浅层单轮省钱），开（完整循环）。网络搜索助手 prompt 改成**模式A/模式B 二选一**。

### 8.3 评测体系（`eval/`）

- **`eval/run_eval.py`**：monkeypatch `api.monitor` 单例记录全部事件 → 走**真实** `run_deep_agent` → 断言。零生产代码改动，跑的就是生产路径。
- **`eval/cases.yml`** 三个 case：
  - `compound_pendulum`：守两个历史 bug（必须调度网络搜索 + 生成文件无 `$$`）
  - `kb_only`：守知识库侧（便宜、快）
  - `deep_research`：守深度循环（≥2 次搜索 + ≥1 次精读 + 带来源 md）
- **用法**：`python -m eval.run_eval`（需先停服务器，8000 端口会被前置检查拒绝）。退出码 0/1/2。
- **关键设计**：断言用**子集语义**（`create_deep_agent` 会自动加 `general-purpose`）；失败重试一次；超时终止整个进程（共享 checkpointer 连接可能脏）；不按 agent 归属计数工具（并行 dispatch 时 report_assistant 在子智能体跑之前就全部发出）。

### 8.4 踩坑实录

#### 问题 19：评测立刻抓到的回归——改完 prompt，模型又写 `$$...$$` 了

- **现象**：Stage 1 重跑评测，`compound_pendulum` FAIL：生成报告里 13 处独立公式全用 `$$...$$`。
- **排查**：评测把文件内容逐行打印出来了——确认不是评测误报，是真实回归。prompt 明明写了"严禁 `$$...$$`"。
- **根因**：**LLM 对 prompt 规则是概率性服从，不是确定性执行**。prompt 能降低错误率，但"0 次违反"保不住。
- **解决**：把"靠 prompt 守格式"改成"落盘前确定性归一化"——`tools/markdown_tools.py` 加 `_normalize_formula_delimiters()`，`generate_markdown` 写文件前统一 `$$...$$`/`\(...\)`/`\[...\]` → `$...$`。回归 PASS。
- **方法论**：**凡是"必须保证"的产出物格式，别用 prompt 守，用工具层兜底**。prompt 是让模型少犯错，兜底是保证不犯错。两者分工：prompt 管"尽量"，代码管"一定"。

#### 问题 20：`re.sub` 写 `\(` `\[` 模式时踩了三个坑

- **现象**：写 `_normalize_formula_delimiters` 时，正则模式里的 `\(` `\[` 一路报 `re.error: unbalanced parenthesis`、`SyntaxWarning: invalid escape sequence`。
- **排查**：三个坑叠加——①`$` 在正则里有特殊含义要 `\$` 转义；②`\(` `\[` 这种"转义括号"不能直接用普通字符串拼，会触发 `invalid escape sequence` 警告；③写测试时 PS 命令行的 `$` 会先被 PowerShell 展开，测试代码混入 `\p` 等假转义。
- **解决**：正则模式全用 **raw string**；测试写进临时 `.py` 文件跑（绕过 PS 的 `$` 展开和引号嵌套），验证后删除。
- **方法论**：**含 `$`/`\`/括号的代码调试，永远用 raw string + 独立脚本文件**，别在命令行交互式拼。这类报错先分「正则语法错」还是「shell 转义错」。

#### 问题 21：后台评测任务随会话切换被终止

- **现象**：评测在后台跑，几次会话上下文切换后任务被 killed（monitor 也超时），`kb_only` 中断。
- **排查**：输出文件只写了 compound_pendulum 的 PASS，后续 case 没跑。
- **根因**：Claude Code 会话切换/上下文压缩时，旧的后台 shell 任务会被终止；monitor 有超时上限（默认 5 分钟）。
- **解决**：重新起评测 + 重挂 monitor（monitor 用长超时 + 具体过滤模式）。
- **方法论**：**长跑任务的监控要能自动恢复**——monitor 超时后重挂；后台任务被终止就重跑。评测/构建这类分钟级任务别假定一次启动就拿到结果。

### 8.5 成本控制：前端「深度调研」开关（深度循环不白烧钱）

深度循环很费 Tavily credit（一次调研可能 8 检索 + 5 精读 ≈ 30+ credits）。直接全开不符合"默认省钱"的诉求，但全关又失去调研能力。方案：**像市面 AI 产品一样，前端一个开关让用户决定**。

**数据流**（全链路透传一个 bool）：
```
web/index.html 开关 → POST /api/task {deep_research: bool}
  → api/server.py TaskRequest.deep_research（默认 False）
  → agent/main_agent.py run_deep_agent(..., deep_research=False)
  → 拼进 query 的【任务模式】指令
  → 主智能体委托网络搜索时在 description 标注开关状态
  → sub_agents.tavily 按 模式A（深度）/ 模式B（浅层）执行
```

**prompt 两段式**（`prompt/prompts.yml` sub_agents.tavily）：
- **模式A·深度调研**：完整循环（多角度→精读→缺口→补检索→带来源汇总），预算 `search ≤ 8`、`extract ≤ 5`。
- **模式B·浅层单轮（默认，省）**：1~2 次搜索即收，`search ≤ 3`，**不精读不追轮**。
- 触发依据：委托 description 含"深度调研开关：已开启"/"综述"/"调研"等，或问题确实需要多轮；否则走模式B。

**评测同步**：`cases.yml` 的 `deep_research` case 显式 `deep_research: true`（否则默认浅层不会精读，min_extract 会挂）；`run_eval.py` 把 case 的 `deep_research` 透传给 `run_deep_agent`。这样评测**同时验证两种模式**。

**成本本质**：开关没有硬编码预算上限——模型对 prompt 是概率性服从，预算上限只是"强烈建议"。模式B 明确"不精读、不追轮"是实践上最有效的省钱杠杆（精读最贵）。若要**确定性**控成本，得在工具层做（如 `extract_web_content` 在浅层模式直接禁用），当前没做——留作进阶选项。

### 8.6 人工审批节点（HITL）：生成报告前人类确认

学习日记"挑战级"遗留项落地。多智能体从"跑完直接出报告"升级为"生成报告前挂起等人工确认"。**关键发现：deepagents 0.4.3 原生支持 HITL，不需要裸写 LangGraph**——这是计划阶段最值钱的调研结论（先读源码再定方案，省掉一大截工程量）。

**原理**（deepagents → langchain 链路）：
- `create_deep_agent(..., interrupt_on={"generate_markdown": {...}})` → 内部挂 `HumanInTheLoopMiddleware`
- middleware 在模型发出工具调用后调 `interrupt(hitl_request)` 抛中断，值 = `HITLRequest`（待审工具调用的 name/args/description，description 支持动态 callable 生成中文摘要）
- 运行时表现：`astream` 遇到中断 → chunk 只剩 `__interrupt__` 键；**同 config（thread_id）`astream(Command(resume=decision), cfg)` 续跑**；approve/reject/edit 三态决策都通
- **resume 依赖 checkpointer**（项目已有 AsyncSqliteSaver）——所以 HITL 和持久化是同一套基础设施，interrupt 是未来更多 human-in-the-loop 场景的地基

**实现**：
- `main_agent.py`：`get_main_agent()` 恒挂 interrupt_on（审批锚点 `generate_markdown`）；`run_deep_agent` 从单段 astream 改成**多轮循环**（首轮 `{"messages":...}` → 遇 `__interrupt__` → `auto_approve=True` 直接批准 / `False` 等审批 → `Command(resume=decision)` 进下一轮）；审批等待用 `asyncio.Future` 全局 dict（`wait_for_approval` / `resolve_approval`），5 分钟超时自动取消。
- `api/server.py`：`POST /api/approve`（`resolve_approval` set_result 唤醒）；`TaskRequest.hitl` 透传 → `auto_approve = not hitl`。
- `web/index.html`：「报告审批」开关（默认开）+ WS 新事件 `approval_required` → 审批卡片（报告预览前 1500 字，textContent 构建防注入，通过/驳回+理由）。驳回后模型收到错误 ToolMessage，会按理由修改重试。
- 评测走 `auto_approve=True`，不等审批 → 回归不受影响。

**headless 验证**（不靠浏览器）：模拟前端流程——`run_deep_agent(auto_approve=False)` 起后台 → 监听 `approval_required` → `resolve_approval(approve)` → 报告落盘确认。approve 路径通过；reject 路径已验证（模型收到驳回理由）。这验证了"中断→等待→resume"完整链路真实可跑。

**面试能聊的点**：interrupt 是 LangGraph 的**原生能力**（不是 deepagents 特有）——`interrupt(value)` 存 checkpoint、`Command(resume)` 续跑、checkpointer 是断点续跑的地基；HITL 三态决策（approve/edit/reject）是产品层"人在回路"的通用模式。评测 + HITL 合起来证明了"概率系统可测、可人为干预"。

### 8.7 PDF 公式渲染失败排查：Word COM 不认 `<img>`（及时止损的教训）

> 2026-08-12 · 想给"md → PDF"补上公式渲染，折腾 4 轮最终砍掉。**最有价值的不是"没做成"，而是"及时判断做不成"**。

**现象**：`convert_md_to_pdf` 生成的 PDF 里公式以 `$T = 2\pi\sqrt{...}$` 源文本显示，不渲染。

**排查链**（每轮都是"换个图片加载方式"，都在同一个错误假设上打转）：
1. 选型：matplotlib mathtext 把 LaTeX 渲染成 PNG（`mathtext.math_to_image` 5/5 公式渲染成功）→ 思路本身可行。
2. 第 1 轮：HTML `<img src="相对名.png">` → PDF 675KB（图片没进去，Word 显示源文本）。
3. 第 2 轮：改成 `<img src="file:///绝对路径">` → PDF 1661KB（变大但仍是源文本）。
4. 第 3 轮：怀疑中文路径 URL 编码（`%E6%A1%8C...`），公式图挪到系统 TEMP 的 ASCII 目录 → PDF 回 675KB，还是不行。
5. **决定性诊断**：把 Word 打开的 HTML 另存为 `.docx`（Word 中间态），`zipfile` 数 `word/media/` 图片数 = **0**。→ 不是路径问题，**Word 从 HTML 导入压根不加载任何 `<img>`**。补测 base64 data URI 方案 → 单独 1 张图 docx 里 media=1（偶然），复杂文档 78 张全 = 0 → data URI 也不可靠。
6. **止损**：砍掉渲染层，回退纯文本转换；`prompts.yml` 告知模型"PDF 公式为源文本，大量公式建议交付 Markdown"。

**根因**：Word COM 的 HTML 导入器对 `<img>` 的支持极差（外部路径、`file:///`、data URI 都不稳定），**"md → HTML → Word → PDF"这条链路天然不支持图片**。这不是缺最后一步，是链路选型错误。

**方法论（这次最大的收获）**：
- **查证中间产物，别在链路上猜**：诊断"Word 到底有没有收图片"的正解是看它自己的中间态（docx 的 `word/media/`），一眼定生死。我前 3 轮都在调 HTML 的 img 写法，本质是在"图片加载方式"这个维度上试错——而正确的第一问是"Word 的 HTML 导入器支持 img 吗"。
- **定义"止损信号"**：如果第 1 次（675KB 明显没嵌图）+ docx media 检查为 0，就该判定"链路不支持"并换方案（或砍），而不是换第 2、3、4 种加载方式。**每次失败后问自己：这轮验证的是新假设，还是旧假设的新写法？** 前 3 轮全是后者。
- **PDF 公式的正解**（如果真需要）：不是 Word COM，而是 **LaTeX/WeasyPrint/Pandoc 这类原生支持数学的渲染器**——它们走的是"排版"路径而不是"HTML 导入"路径。Windows 上最务实的是装一个本地 TeX 发行版或换引擎，代价是系统级依赖。学习项目不值得为此装 TeX，所以砍掉，用 prompt 诚实标注限制。

---

*这份日记的价值在"怎么排查"而不在"做了什么"。下次遇到问题，先回到第四节的方法论。*
